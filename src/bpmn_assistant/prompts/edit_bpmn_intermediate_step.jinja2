Updated process:
For diagrams containing subprocesses or boundary handlers, continue with
`replace_process` and the complete updated process, preserving unchanged scopes,
IDs, handlers, timers, and expansion settings. Otherwise use the previously described functions.
```json
{{ process }}
```

If you believe you have completed ALL the necessary changes and want to stop editing the process, output ONLY this JSON structure:
```json
{
  "stop": true
}
```

Otherwise, you may continue editing the process using the previously mentioned functions.
